{
  "name": "adblock-monorepo",
  "private": "true",
  "scripts": {
    "build": "yarn --cwd test-app build"
  },
  "workspaces": [
    "adblock-detect-react/**",
    "test-app/**"
  ],
  "resolutions": {
    "immer": "^8.0.1"
  }
}
