{
  "name": "adblock-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "preinstall": "test \"$CI\" = true && npx pnpm install -r --store=node_modules/.pnpm-store || npx only-allow pnpm",
    "prebuild": "test \"$CI\" = true && npx pnpm install -r --store=node_modules/.pnpm-store || echo skipping pnpm install",
    "build": "turbo run build",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "turbo": {
    "npmClient": "pnpm",
    "pipeline": {
      "build": {
        "dependsOn": [
          "^build"
        ],
        "outputs": [
          "cjs/**",
          "esm/**",
          "dist/**"
        ]
      },
      "lint": {}
    }
  },
  "resolutions": {
    "immer": "^8.0.1"
  }
}
