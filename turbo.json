{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["cjs/**", "esm/**", "dist/**"]
    },
    "clean": {
      "cache": false
    }
  }
}